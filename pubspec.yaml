name: happy_weds_vendors
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.1.1
  image_picker: ^1.1.1
  video_player: ^2.7.0
  url_launcher: ^6.2.5
  youtube_player_flutter: ^9.0.1
  share_plus: ^10.0.5

  cupertino_icons: ^1.0.8
  fl_chart:
#  connectivity_plus:
  flutter_launcher_icons:
  http:

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter_icons:
  android: true
  ios: true
  image_path: "assets/icons/hwav.webp"
  country_state_city: ^latest_version



  flutter_lints: ^5.0.0


flutter:


  uses-material-design: true
  assets:
    - assets/images/logoo.png
